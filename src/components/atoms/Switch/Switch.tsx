import {
  Switch as NextUiSwitch,
  useSwitch as NextUiUseSwitch,
} from "@nextui-org/switch";

import type { SwitchProps as NextUiSwitchProps } from "@nextui-org/switch";

export type SwitchProps = NextUiSwitchProps;

export const Switch = NextUiSwitch;
export const useSwitch = NextUiUseSwitch;
